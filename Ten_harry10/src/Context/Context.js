import { createContext } from "react";

export const countContext= createContext(0)


// Purpose: createContext is used to create a new Context object. This object contains two components: a Provider and a Consumer.

